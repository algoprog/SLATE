# Core dependencies
accelerate
codetiming
datasets
dill
hydra-core
numpy<2.0.0
pandas
peft
pyarrow>=19.0.0
pybind11
pylatexenc
ray[default]>=2.41.0
tensordict>=0.8.0,<=0.10.0,!=0.9.0
torchdata
transformers
wandb
packaging>=20.0
tensorboard

# vLLM for rollout and judge server
vllm>=0.8.5,<=0.11.0

# GPU acceleration
liger-kernel
flash-attn

# Retrieval server
uvicorn
fastapi

# LLM judge communication
requests
aiohttp

# Evaluation
tqdm
